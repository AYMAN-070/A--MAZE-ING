# A-MAZE-ING

# General Rules
- Include docstrings in functions and classes
- include type hints for function parameters, return types, and variables where applicable
- handle exceptions gracefully
- manage ressources to prevent leaks
- Use mypy for static type checking